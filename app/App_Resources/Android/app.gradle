// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "org.drinkapp"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
